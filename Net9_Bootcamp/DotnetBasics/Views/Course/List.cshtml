@* @model Course[] *@
@* @model IEnumerable<Course> *@
@model List<Course>

<h1 class="h3 mb-3">Tüm Kurslar</h1>
<div class="row">

    @foreach (Course course in Model)
    {
        <div class="col-md-3">
            @await Html.PartialAsync("_CourseCard", course)
        </div>
    }

</div>